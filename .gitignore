# Local scratch/source dumps
tmp/
scratch/

# Environment and secrets
.env
.env.*
!.env.example
# Dummy Supabase values only, for `npm test` -- see the comment in the
# file itself. Never a real credential; safe (and necessary) to commit
# so a fresh clone/CI can actually run the test suite.
!.env.test

# Dependencies/build output
node_modules/
.next/
.vercel/
dist/
build/
coverage/
*.tsbuildinfo
vite.config.js
vite.config.d.ts
vitest.config.js
vitest.config.d.ts
dev-server.log
test-results/
playwright-report/
blob-report/
playwright/.cache/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS/editor noise
.DS_Store
Thumbs.db
.vscode/
.idea/
.git-credentials-ergon

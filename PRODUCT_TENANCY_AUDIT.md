# Ergon Productization Audit — Tenant Isolation & Configuration Baseline

Status: Read-only audit complete; §10's twelve open questions now carry working decisions (E, 2026-09-08); two previously-open verification items (§7) and the exact admin roster (§1) are now directly confirmed. No production code, database policy, migration, workflow, or business process has been changed to produce this document or its updates. See `PRODUCT_PHASE1_PLAN.md` for the first concrete (not-yet-implemented) implementation plan built against these working decisions.
Created: 2026-09-08 · Working decisions and verification updates added: 2026-09-08
Owner: Ergon leadership (decisions), engineering (implementation once decisions are made)
Scope authority: `PRODUCT_PLAN.md` (product direction), `PRODUCT_START_PLAN.md` (this is deliverable #2 in its "First deliverables" list — the "Productization and tenant-isolation gap audit")

## How to read this document

Three kinds of statement appear here, and they are kept visually distinct on purpose:

- **Confirmed current behavior** — read directly from the actual SQL migrations, server route code, or frontend source, with a file:line citation. Not inferred, not assumed. Every row's "Evidence" column is this kind of statement.
- **Inferred risk** — what the confirmed behavior would mean *if* a second company's data existed in the same database today. These are logical consequences of the confirmed behavior, not separately verified by creating a second company (no second company exists; none should be created before the work in this document's own §9 is complete).
- **Recommendation / proposed direction** — this document's own proposal for what to do about a finding. Nothing in a "Proposed Direction" or "Decision Needed" column has been approved, scheduled, or implemented. Per `PRODUCT_PLAN.md`'s process-protection rule, no existing business process is changed by this document; per `PRODUCT_START_PLAN.md`, no mechanical schema change (e.g. "add a workspace column everywhere") is authorized here either.

## Method

Three parallel, read-only research passes were run against the actual repository:

1. **Database**: every one of the 114 SQL migration files in `backend/supabase/migrations/` (`001_initial_ops_schema.sql` through `114_trusted_notification_creation.sql`), read in full — every `create table`, all 253 `create policy` statements, every `security definer` function, every storage bucket/policy.
2. **Server**: every file under `api/` (8 route files), `api/_lib/` (8 shared helpers), `api/cron/` (1 job), and `vercel.json`.
3. **Frontend**: `src/main.tsx` (25,213 lines) and `src/persistence.ts` (10,680 lines) — hard-coded identity/branding, global constant arrays, the Admin panel's actual configurable surface, search, notifications, localStorage, reporting/exports, and seed/demo data.

Every finding below is traceable to one of these three passes or to a direct follow-up read done to close a specific gap (noted where relevant). No file was edited during this audit.

---

## 1. Single-company, singleton, hard-coded-company, and default-workspace assumptions

| Area | Current Behavior | Evidence | Product Gap | Risk | Proposed Direction | Decision Needed |
|---|---|---|---|---|---|---|
| Company identity table | `company_branding` is a literal Postgres singleton — a second row is structurally impossible | `backend/supabase/migrations/039_company_branding.sql:7-13`: `id boolean primary key default true ... constraint company_branding_singleton check (id)`. The migration's own comment states the design intent: *"there is exactly one 'the company' for a given deployment of this app."* | No way to add a second company's name/logo without a second deployment or a schema change | High — this is the clearest first-party evidence the current architecture is "one company per deployment," not multi-tenant-in-one-database | Convert to a `workspaces` table (one row per company), migrate the existing singleton row's values into the first workspace row (see §8) | Confirm no other part of the app depends on `company_branding.id = true` as a literal join target before converting |
| Global admin model | `app_admins`/`app_user_roles` have no company column; membership makes a user an admin/role-holder for the **entire database**, not one company. **Verified 2026-09-08 via a direct, authenticated, read-only query against production**: exactly **one** row exists in `app_admins` today — `eck1679@gmail.com` (`user_id 593b9540-a1dc-497e-a4c2-bc43c5c9b561`, admin since 2026-08-03). No other account currently holds admin status. | `app_admins` (`012_admin_roles_and_user_directory.sql:7-10`), `is_app_admin()` (`012:12-19`, `select 1 from app_admins where user_id = check_user_id`), `app_user_roles` (`010_user_role_assignments.sql:3-7`, widened in `040_role_expansion_multi_role.sql:8-22` to one row per (user_id, role_key) with an `is_primary` flag — not one row per user) | No concept of "admin of Company A but not Company B" | Critical — under the current model, any company's admin would automatically be an admin of every other company sharing this database | Introduce `workspace_members(id, workspace_id, user_id, role_key, is_primary, is_workspace_admin)`, mirroring `app_user_roles`' existing one-row-per-role shape; keep a separate, much smaller `platform_admins` table for Ergon's own platform-level staff | **Decided as a working default (E, 2026-09-08)**: today's `app_admins` row(s) become admins of the first workspace (holding Ergon/Ensight's real operational data, labeled "Ergon Test Workspace" as of the 2026-09-08 naming correction — see `PRODUCT_PHASE1_PLAN.md`); platform-admin access stays separate and is granted only to specifically approved designated Ergon product operators. Since exactly one admin account exists today, no roster ambiguity exists for the workspace-admin migration itself — see the Phase 1 plan (`PRODUCT_PHASE1_PLAN.md`) for the exact migration step. **Still requires confirmation**: who (if anyone, beyond `eck1679@gmail.com`) should hold platform-admin status — this was deliberately NOT assigned as part of this pass, per E's explicit instruction. |
| Notification preferences | `notification_rules.event_type` carries a database-wide `unique` constraint — one row per event type, period | `024_notification_rules_engine.sql:10`: `event_type text not null unique check (...)` | Two companies cannot have different notification preferences for the same event type | High (config-bleed, not data-bleed) — Company A silencing an event type silences it for Company B too | Composite key `(workspace_id, event_type)` unique instead of `event_type` unique alone | — |
| Global "programmable" tables with no scoping | `standard_install_times`, `presales_hardware_rules`, `site_hardware_rules` all use globally-unique natural keys with no scoping column | `025_...sql:29-33` (install times), `028_...sql:26-27` (presales rules), `045_...sql:29-30` (site hardware rules) | Same shape as notification_rules — one shared configuration library for whatever companies share the database | Medium — config-bleed, and could silently apply one company's install-time/hardware assumptions to another's estimate | Same composite-key pattern as notification_rules | — |
| Global uniqueness on business-identity columns | `vendors.name`, `locations.name`/`code`, `projects.project_name`, `inventory_items.sku`, `purchase_orders.po_number`, `equipment_types.equipment_number`/`equipment_name`, `product_catalog.catalog_number`, `team_members(lower(email))`, `clients.name`, `sales_quotes.quote_ref` are all globally unique today | `001_initial_ops_schema.sql:13,33,46-47,58,79,95`; `003_...sql:13`; `020_...sql:20-21`; `013_...sql:10`; `019_team_roster.sql:23-25`; `102_...sql:34`; `066_...sql:90` | A second company could not register a vendor/location/project/client with the same *name* as an existing Company A record; **could never onboard a team member who shares an email address with any Company A team member** | High for the `team_members` email case specifically (a very plausible real-world collision — a shared contractor, or simply two different people who happen to work for two client companies and each use their employer's standard email format) | Every listed constraint becomes `unique(workspace_id, x)` | Should some of these (e.g. a vendor genuinely shared across two companies) remain cross-workspace, or should every one become strictly per-workspace? |
| Reference-number generation | `sales_quote_ref_counters`/`project_ref_counters` are keyed only by calendar `year` (not company); `SKU-`/`BLD-` refs are computed client-side as `max(existing refs) + 1` with the year literally hard-coded into the regex | `066_...sql:14-17` (`year integer primary key`), `067_...sql:17-20` (same); `src/main.tsx:1059-1075` (`nextSkuRef`/`nextBuildRef`), `src/main.tsx:11872-11879` (`nextProjectRef`, regex `/^PRJ-2026-(\d+)$/` — the year `2026` is a literal, not derived from the current date) | Two companies would draw project/quote reference numbers from the same year-keyed sequence, producing either collisions or an ambiguous shared numbering space | High for the DB-backed counters (real collision risk); the client-side max+1 pattern for SKU/BLD refs is *also* a pre-existing single-company concurrency bug (two users creating a SKU simultaneously can already collide today), independent of tenancy | Composite PK `(workspace_id, year)` for the DB counters; move SKU/BLD numbering to the same trigger-based counter pattern already used for quotes/projects (066/067) instead of client-side max+1 | The hard-coded `2026` in `nextProjectRef`'s regex is a live, single-company bug worth its own small fix regardless of the tenancy timeline — flagging separately, not proposing to fix during this read-only audit |
| Hard-coded company/domain identity | `support@ensight-technologies.com` is hard-coded into the Web Push VAPID subject; `"Ergon Ops"` is the hard-coded default/fixed sender identity in five server files; real Ensight legal proposal text is seeded as row data | `api/send-push.js:200`; `api/_lib/mailer.js:38`; `api/send-invite-email.js:46`; `api/send-notification-email.js:91` (not even overridable); `api/send-proposal-email.js:101`; `api/send-submittal-email.js:99`; `backend/supabase/migrations/053_sales_quote_proposals.sql:60-96` (six paragraphs of "EnSight"/"ENSIGHT"-named contract boilerplate) | Every outbound push/email always identifies as Ergon/Ensight regardless of which company's data triggered it; client-facing legal text for a second company would need to be manually rewritten with no structural separation from Company A's | High for brand/identity correctness once a second company exists (not a data leak, but a serious first-impression/support-routing problem — a Company B customer receiving a proposal signed "Thanks, Ergon Ops" or a push notification whose contact-of-record is Ensight's own support email) | Move sender identity, VAPID subject, and proposal boilerplate to per-workspace settings; the current global values become the default for the first (Ergon) workspace only | — |
| Dead code still shipping real client data | A top-level `const projects: ProjectSite[]` array in `main.tsx` (lines 906-991) still contains real named clients ("Newport News Shipbuilding," "Straub Medical" with a real street address). **Verified 2026-09-08, safe to delete**: two independent grep passes (a broad `\bprojects\b` scan and a narrower `projects\.(find|map|filter|...)` / `projects\[` scan, to rule out property-access false negatives against the unrelated `globalSearchMatches.projects`/view-key string usages) confirm the module-level array has **zero live references anywhere in the file** outside its own declaration. The one place it was formerly read as a fallback (`?? projects[0].name`) was already replaced with `?? projectSites[0]?.name` — confirmed by the comment at `main.tsx:11686-11692` explaining exactly why. | `src/main.tsx:906-991` (declaration); confirmed zero other references via two independent grep patterns, 2026-09-08 | Not a functional gap, but a real client's name/address ships to every browser that loads the app today, and would ship to a resold copy of this codebase too | Low, now that deletion is confirmed safe — this was previously flagged Medium pending verification | **Verified safe to delete; deletion itself not yet performed** (this pass was scoped to verification only, per E's explicit instruction) | Confirmed as a working default (E, 2026-09-08): "Remove the unused source-code array containing real client information." Ready to execute as a small, isolated, zero-risk cleanup whenever E gives the go-ahead to make the actual code edit. |
| Live Emerald Queen demo fallback | `emeraldQueenImportedProject` (`main.tsx:865-904`) is a hard-coded object with real Ensight proposal content (client name, address, BOM, pricing narrative), still live: triggered only when a user uploads a file whose name contains "emerald queen" (case-insensitive) **and** the real `/api/sales-quote-extract` call fails (`main.tsx:12303-12306`) — a narrow, specific combination. **Verified 2026-09-08, safe to replace**: (1) a second, separate grep for the literal string "Emerald Queen" anywhere in `main.tsx` confirms it appears *only* inside this one object's own field values and the one narrow trigger check — no report, lookup, or other code path depends on its exact name/address/BOM content; (2) a live check of the actual production Projects list (`https://ergon-ops-app.vercel.app/#projects`, 2026-09-08) confirms the 5 real projects that exist today are East Central Garage, Exxon Mobil, Lakeside Gate, NNSB 37th Street, and Straub Medical HI — **no project named "Emerald Queen" exists in production**, confirming no live record depends on or was ever created from this exact fallback content; (3) architecturally, changing this source constant only affects computations that run *after* a future deploy — it cannot retroactively alter any project row already saved to the database under a previous version of the constant, for the same reason Proposal/Schedule Template edits are already safe (see §7). | `src/main.tsx:865-904,12303-12324`; live production Projects list checked 2026-09-08 | Real second-company-facing content (fictional though it will become) is currently gated behind a code path that is easy to forget exists, since it only fires on AI-extraction failure | Low, now that both the isolation and the no-live-dependency checks are confirmed | **Verified safe to replace with fictional sample data; the replacement itself not yet performed** (verification-only pass) | Confirmed as a working default (E, 2026-09-08): "Replace the still-active Emerald Queen fallback with clearly fictional sample data." Ready to execute — swap the object's field values (client/address/BOM/pricing) and the trigger string for generic fictional equivalents — whenever E gives the go-ahead. |
| Vertical-specific taxonomy hard-coded as code, not data | `CATALOG_CATEGORY_OPTIONS` (10 fixed categories, including the literal `"EnSight Kits"`), `CATALOG_CATEGORY_FIELDS` (camera/signage/VPU-specific spec fields), and `SITE_HARDWARE_METRIC_OPTIONS` (FLI/LPR/people-counting — parking/traffic-camera-specific) are fixed TypeScript constants, not admin-configurable | `src/main.tsx:18543-18558,18598-18644,19457-19464` | A company outside the parking/traffic-camera vertical could not use these Admin panels to fit its own product taxonomy — it would need source changes | Medium — this is a real product-design question (how "vertical" is Ergon meant to be?), not just a technical gap | Treat as a deliberate scope question, not an automatic "make it all free-text" conclusion — see §10 | Should catalog categories/metrics become fully admin-defined free-text, or should Ergon ship a "parking & traffic" starter template alongside a blank template for other verticals? |
| Global constant arrays defining app structure | `ROLE_KEY_OPTIONS` (10 fixed roles), `DEFAULT_TABS_BY_ROLE`, `TASK_STATUS_OPTIONS`, `PURCHASE_ORDER_STATUSES`, `TASK_PRIORITY_OPTIONS`, and the server-side notification event-type allowlist (`HANDLERS` in `notificationEvents.js`) are all fixed in source | `src/main.tsx:16793-16804` (roles), `514-525` (default tabs), `585-591` (task status), `593-598` (priority); `src/persistence.ts:7417-7454,7563` (PO status); `api/_lib/notificationEvents.js:155-404` (`SUPPORTED_EVENT_TYPES`) | None of these can be extended/renamed by an admin — see §6 for the full "requires programming" list | Low direct security risk, but a real product-completeness gap for a company whose org structure or workflow doesn't match Ergon's current 10 roles / 5 task statuses | See §6 | — |
| Single global external-service credentials | OpenAI (quote extraction), Gmail/Resend (email), Slack bot token/webhook, VAPID push keys, and `UPSTASH_REDIS_REST_URL`/`CRON_SECRET` are all single global Vercel environment variables | `api/sales-quote-extract.js:15,272,279`; `api/_lib/mailer.js:22,29-30,55-56`; `api/send-notification-slack.js:101-102`; `api/send-push.js:121-122`; `api/_lib/rateLimit.js:27-31` | One company's paid AI-extraction usage, one company's email-sending identity, one company's Slack workspace/Teams channel serves every company sharing this deployment | High for cost attribution and brand identity once a second company exists (Company B's AI extraction calls bill to Company A's OpenAI key; Company B's notifications post to Company A's Slack) | Per-workspace integration settings table, env vars become the default/fallback for the first workspace only | Which integrations, if any, should Ergon offer as a shared/default provider for smaller customers vs. requiring bring-your-own credentials from day one? (See §10.) |
| Outbound-link allow-list | `api/_lib/validateUrl.js:11` hard-codes `STATIC_ALLOWED_HOSTS = ["ergon-ops-app.vercel.app", "localhost", "127.0.0.1"]` | `api/_lib/validateUrl.js:11,27-28` | A second company on its own custom domain would need this array edited and redeployed before its outbound links (e.g. proposal/submittal share links) would validate | Low-medium — a real launch blocker for a second company's own domain, not a data-exposure risk | Move to a per-workspace allowed-domains list, or derive from `workspaces.custom_domain` | — |
| `workspace_key` column | `app_records`, `app_state_snapshots`, `app_sync_events`, `app_transaction_locks` all carry a `workspace_key text default 'default'` column that is hard-coded to the literal string `'default'` everywhere it's used | `008_...sql:7,17`; `003_...sql:104`; `009_...sql:6`; confirmed hardcoded via backfill queries in migrations 016-022 filtering `where ar.workspace_key = 'default'`; `src/persistence.ts:26` (`WORKSPACE_KEY = "default"`, confirmed unused for real storage-key namespacing) | Looks like a tenant key at first glance; is not one | Low — this is a legacy vestige of an early no-login MVP sync design, not a real tenancy mechanism, and does not need to be reused for the real workspace model | Do not reuse `workspace_key` as the real tenant column — it is legacy plumbing, name-collision risk aside | Confirm `app_records`/`app_state_snapshots` are still load-bearing anywhere before deciding whether to retire or repurpose this column |

---

## 2. Which database records have explicit company/workspace ownership, and which do not

**Confirmed current behavior: none.** All 86 tables in the schema were enumerated directly from their `create table` statements (see the database audit's full table for the complete list, file-by-file). Not one has a `company_id`, `workspace_id`, `tenant_id`, `org_id`, or semantically equivalent column. A repository-wide search for those four column-name patterns across every migration file returned zero matches.

| Area | Current Behavior | Evidence | Product Gap | Risk | Proposed Direction | Decision Needed |
|---|---|---|---|---|---|---|
| Every application table (all 86) | No table has any company/workspace/tenant column | Full table-by-file inventory produced during this audit (86 tables, `001` through `113`); zero `company_id`/`workspace_id`/`tenant_id`/`org_id` matches anywhere in `backend/supabase/migrations/` | No way to express "this row belongs to Company A" anywhere in the schema today | Critical — this is the foundational gap everything else in this document follows from | Add `workspace_id` incrementally, staged by table group, per §8/§9 — not mechanically to every table at once | Confirm the staged table-group order against the "shared records" list in `PRODUCT_PLAN.md` (see the cross-reference table below) |
| Closest-to-ownership concepts that exist but are **not** tenant boundaries | `departments` is an internal org-chart lookup table (e.g. "Warehouse," "Sales"), not a company boundary; `clients` is Ensight's own customer list (a CRM record, itself a candidate for workspace-scoping, not a scoping mechanism for other tables); `workspace_key` is a hard-coded legacy MVP-sync string (see §1) | `backend/supabase/migrations/001_initial_ops_schema.sql:11-18` (`departments`); `102_...sql:32` (`clients`); `022_...sql:5-9` (comment noting `projects.department_id`/`owner_id` were explicitly abandoned as a scoping mechanism in favor of plain columns) | None of these can be pressed into service as a stand-in for a real workspace column | Medium — a risk only if someone mistakenly treats one of these as "good enough" tenancy scoping later | Build the real `workspaces`/`workspace_id` model; do not repurpose `departments`, `clients`, or `workspace_key` for tenant isolation | — |

**Cross-reference against `PRODUCT_PLAN.md`'s "Shared records that connect the lifecycle" (15 items) — what actually exists today:**

| PRODUCT_PLAN.md concept | Confirmed existing table(s) | Status |
|---|---|---|
| Client | `clients` | Exists, unscoped |
| Contact | — | **No dedicated table found.** `team_members`/`app_known_users` partially cover internal people; no external customer-contact table was found in any of the three audits. |
| Site | `locations`, `project_locations`, `sales_quote_locations` | Exists (split across three tables), unscoped |
| Opportunity | — | **Not found.** No CRM pipeline/opportunity table exists in the current schema — matches `PRODUCT_PLAN.md`'s framing of this as future scope. |
| Quote and accepted version | `sales_quotes`, `sales_quote_proposals` | Exists, unscoped |
| Product/service/equipment catalog | `product_catalog`, `equipment_types`, `equipment_bom_components` | Exists, unscoped |
| Location and system design | `locations`, `project_locations`, `sales_quote_locations`, `project_location_items` | Exists, unscoped |
| Billing account and Client Ledger | — | **Not found as a dedicated table.** Migration `089_client_ledger.sql` creates only `installed_assets` — "Client Ledger" appears to be a UI-computed view over existing project/asset data, not its own table (confirmed by direct follow-up read of `089_client_ledger.sql`, which contains no `client_ledger`-named table). |
| Project | `projects` | Exists, unscoped |
| Installed asset | `installed_assets` | Exists, unscoped |
| SaaS subscription / service entitlement | — | **Not found.** Matches HANDOFF.md's own characterization of SaaS as "placeholder." |
| Document and document version | `project_documents` | Exists (no separate version-history table found), unscoped |
| Task, approval, responsibility | `tasks`, `catalog_price_change_requests`, `purchase_order_holds` | Exists, unscoped |
| Communication and activity timeline | `conversations`, `direct_messages`, `channels`, `channel_messages`, `task_activity_log` | Exists, unscoped |
| Support case | — | **Not found.** Matches `PRODUCT_PLAN.md`'s framing of Service/Support as future scope. |

This table is itself a finding: roughly half of the 15 "shared records" `PRODUCT_PLAN.md` calls for already exist in some form (unscoped); the other half (Contact, Opportunity, Billing account, SaaS subscription, Support case) do not exist yet at all, which means they can be **designed with workspace scoping from day one** rather than retrofitted — a meaningfully lower-risk path than the tables that already have real production data.

---

## 3. Current RLS, API, Storage, search, notification, scheduled-job, cache, export, and reporting boundaries

| Area | Current Behavior | Evidence | Product Gap | Risk | Proposed Direction | Decision Needed |
|---|---|---|---|---|---|---|
| Row-Level Security (RLS) | 253 `create policy` statements exist across all migrations; 137 contain `using (true)` and 66 contain `with check (true)`. The dominant pattern for most business tables (departments, vendors, locations, projects, inventory, purchasing, sales, tasks, documents, shipments, and more) is, and largely remains, fully open to any authenticated + approved user | Full policy-by-policy classification produced during this audit; see the "fully open to authenticated for write" table (30+ tables) and the "later tightened?" cross-check (only `notifications` INSERT was closed for real security reasons, in migration `114`, dated 2026-09-07) | Role-based tightening (Phase 9, migration `023`; catalog gate, migration `033`) improved *who within one company* can write what — it did not, and structurally could not, add a tenant boundary, since no `company_id` column exists to reference | Critical — every "authenticated-only" policy today would grant full read/write across companies if two companies shared these tables | Add the workspace predicate alongside (not instead of) existing role checks, table-group by table-group, per §9 | — |
| Security-definer functions | `is_app_admin`, `is_app_manager`, `has_role`, `get_users_by_role`, `get_admin_emails`, and every public/anon token-lookup function (`get_submittal_by_token`, `get_quote_proposal_by_token`, `get_invite_by_token`, `respond_to_submittal`, `respond_to_quote_proposal`, `accept_invite`) query their tables with no company predicate — `get_users_by_role`/`get_admin_emails` in particular are designed to return **every** matching user in the entire installation | `042_...sql:16-26`; `049_...sql:7-17`; full function-by-function evidence table produced during this audit | These functions are the actual recipient-resolution mechanism behind most notification types (`task_assigned`, `purchase_request_status_changed`, `build_stage_changed`, `catalog_price_change_requested/reviewed`, `user_signup_pending`) | Critical — this is the most concrete "could actually cross-notify" mechanism found: today, `roleEmails(ctx, "warehouse")` returns every warehouse-role user in the database, full stop | Every such function needs a `workspace_id` parameter and a corresponding predicate before any second workspace can safely use notification-triggering events | — |
| Server API auth model | Every route uses `requireAuth` (proves "a real signed-in user of this one Supabase project") and, where role-gated, `requireRole` against the same global `app_admins`/`app_user_roles` tables described in §1. Zero of the 8 routes' Supabase REST calls include a company/workspace filter | `api/_lib/requireAuth.js:19-24` (its own comment: *"This only proves 'a real logged-in Ergon user is asking.'"*); `api/_lib/requireRole.js:39-40,52-61`; confirmed absent from every route's REST call in the full route-by-route table produced during this audit | No route can express "and only within the caller's own company" because there is no company concept to check against | Critical | Every route resolves the caller's `workspace_id` (from a JWT claim or an explicit header, see §8) and passes it into every downstream query and into `requireRole`'s authorization check | Exact mechanism for how a route learns "which workspace is this request for" (see §8/§10) |
| Scheduled jobs | The one cron job, `api/cron/task-overdue.js`, scans the **entire** `tasks` table (`status=neq.done&due_date=lt.today&assignee_email=not.is.null`) using the service-role key, which bypasses RLS entirely regardless of any future RLS tightening | `api/cron/task-overdue.js:89-92`, confirmed via `vercel.json:2-7` (the only registered cron) | A future scheduled job copying this pattern would have no company boundary to fall back on even after RLS is tightened, since service-role bypasses RLS by definition | High — not because this specific job cross-notifies today (each notification still goes to that exact task's own assignee), but because it establishes the pattern every future job will likely copy | Add an explicit `workspace_id` filter (or a per-workspace loop) to every service-role-key job, since RLS cannot be relied on to protect service-role queries | — |
| Storage | 9 buckets exist (`project-documents`, `sales-quote-images`, `company-branding`, `catalog-datasheets`, `project-location-images`, `project-shipment-photos`, `purchase-order-files`, `message-attachments`, `avatars`). All but one (`message-attachments`, participant/channel-scoped) grant full CRUD to any authenticated user, scoped only by `bucket_id` | `031_...sql:15-34` (project-documents; its own comment: *"matching the same 'authenticated read/write' posture already used for every other table in this schema"*); full bucket-by-bucket table produced during this audit | No per-project, per-conversation (outside message-attachments), or per-company boundary on 8 of 9 buckets | Critical — any signed-in user from any company sharing this deployment could read/write any other company's project documents, sales-quote images, catalog datasheets, purchase-order files, shipment photos, and location images | Introduce a workspace-scoped path-prefix convention (`<workspace_id>/...`) and rewrite each bucket's policies to check that prefix against `workspace_members`, or move to one bucket per workspace | Path-prefix vs. per-workspace-bucket — see §10 |
| Search | Entirely client-side; no server search endpoint exists anywhere in `api/`. Global search (`globalSearchMatches`, `main.tsx:5525-5570`) filters already-loaded, unfiltered in-memory arrays fetched by `persistence.ts` with the user's own token and no company predicate. Message search (`persistence.ts:1763-1787`) queries Supabase live but relies entirely on RLS, with its own comment confirming this: *"Doesn't need to know which conversations the user is in — RLS ... already restricts every row returned."* | `src/main.tsx:5525-5570`; `src/persistence.ts:1763-1787` | Search inherits whatever the underlying data-load queries and RLS allow — today, that is "everything in the shared tables" | Critical if two companies' rows existed in the same tables — search would return them indiscriminately | Once `workspace_id` and workspace-scoped RLS exist (§8/§9), search inherits the fix automatically since it reads through the same data-load queries and RLS — no separate search-specific fix should be needed | Confirm this "inherits the fix" assumption once the underlying RLS work lands, rather than assuming it without re-testing search specifically |
| Notifications | Covered in depth in §1/§4; `notification_rules` is a single global table, and the recipient-resolution RPCs (`get_users_by_role`/`get_admin_emails`) are app-wide with no company filter | See above | — | Critical (see §4) | See §1/§8 | — |
| Cache (localStorage) | All `ergon:*` localStorage keys were enumerated. Most are trivial UI state (scroll position, collapsed sections, last route) or already user-scoped (`ergon:collapsed-channel-groups:<userId>`, `ergon:pinned-messages:<userId>`). The one exception, `ergon:last-thread`, is not explicitly user-keyed, but localStorage is inherently per-browser-profile already | Full key-by-key table produced during this audit | `ergon:last-thread` is a minor UX inconvenience only if two different users share one physical browser profile — a pre-existing single-tenant edge case, not one that gets worse with multi-tenancy | **Low** — this is the one area of the ten requested where the audit's own conclusion is "not a real risk," stated plainly rather than inflated to match the severity of the other findings | No action needed for tenancy purposes specifically | — |
| Reporting / exports | The Reports component and Dashboard tiles ("Inventory Value," "Recent Order Spend") aggregate over the same fully-loaded, unscoped in-memory arrays used by search. CSV export (`exportCsv`, `main.tsx:1219`) exports whatever was already filtered client-side from that same unscoped data | `src/main.tsx:1471,1473` (`inventoryValue`/`openPoValue` raw `reduce()` over entire arrays); full report-by-report table produced during this audit | No report or export has any company-scoping filter, because no such column exists upstream | Critical, and the **most immediately visible** breakage of any finding in this document — per the audit's own assessment, the Dashboard's "Inventory Value"/"Recent Order Spend" tiles and the Vendor/Project Spend reports would show a single blended dollar figure mixing both companies' data the instant a second company's rows existed, visible to any user without needing to know anything about the schema | Same fix as search — inherits correctness once `workspace_id` + RLS + query-level filtering are in place; recommend an explicit regression test on these specific tiles given how visible a failure here would be | — |

---

## 4. Anything that could expose or mix information between future customer companies

This section pulls together the specific *mechanisms* of cross-company exposure implied by §§1-3, ranked by how directly they would leak information (vs. just "wrong-looking aggregate numbers"):

| Area | Current Behavior | Evidence | Product Gap | Risk | Proposed Direction | Decision Needed |
|---|---|---|---|---|---|---|
| Cross-company admin visibility | Any company's admin (`is_app_admin` = true) can read `app_known_users` (the full directory of every signed-in user, any company), manage `app_admins` (grant/revoke admin for any user, any company), and write `notification_rules` (affecting every company) | `012_...sql:50-52` (`"users and admins read directory"`, `using (auth.uid() = user_id or is_app_admin(auth.uid()))`); `012:23-30` | No separation between "platform admin" and "company admin" — see §1 | Critical | Split into `platform_admins` (Ergon's own staff) and `workspace_members.is_workspace_admin` (per-company) | — |
| Notification recipient resolution crossing companies | `get_users_by_role`/`get_admin_emails` return every matching user app-wide; used directly inside `task_assigned`, `purchase_request_status_changed`, `build_stage_changed`, `catalog_price_change_requested/reviewed`, `user_signup_pending` notification handlers | `api/_lib/notificationEvents.js` (per-handler citations in the server audit) | A purchase-request status change in Company A would notify every "purchasing"-role user in the database, including Company B's | Critical — this is a genuine, specific cross-company data leak mechanism, not just a theoretical schema gap | Every RPC call from `notificationEvents.js` needs a `workspace_id` argument | — |
| Storage buckets | 8 of 9 buckets grant full CRUD to any authenticated user with no per-project or per-company check | See §3 | A Company B user could browse/download Company A's project documents, sales-quote images, purchase-order files, etc., by bucket alone | Critical | See §3/§8 | — |
| Public/anon token-based functions | `get_submittal_by_token`, `get_quote_proposal_by_token`, `get_invite_by_token`, and their `respond_to_*` counterparts are `security definer` and query their tables app-wide once a token resolves | `025_...sql:142-198`; `053_...sql:147-203`; `041_...sql:49-121` | A leaked or guessed token today already exposes one row anywhere in the app (a pre-existing risk independent of tenancy); multi-tenancy sharpens this only in that the blast radius description ("one submittal," "one quote") becomes "one submittal or quote belonging to some company, we don't know which, from the token holder's perspective" | Medium — this is already a risk today for a single company; it does not get categorically worse with tenancy, but it is worth re-verifying token entropy/expiry once real customer-facing tokens exist across multiple companies | No change proposed beyond what §8 already does (adding `workspace_id` to the underlying tables closes this incidentally) | Should share tokens carry an explicit expiry if they don't already? (Out of scope for this audit to confirm — flagging as a follow-up question, not a finding.) |
| Global uniqueness collisions | See §1 — `team_members` email uniqueness is the most likely real-world collision | See §1 | A second company could be blocked from adding a real team member simply because that email already exists for Company A | High (functional blocker, not silent data leak) | See §1 | — |
| Reference-number collisions | See §1 — `project_ref_counters`/`sales_quote_ref_counters` keyed only by year | See §1 | Two companies' `PRJ-2026-0004` could refer to entirely different projects, creating real confusion in any place a ref number is used as a human-facing identifier (emails, documents, conversations) | High | See §1/§8 | See §10 (is per-workspace numbering starting at 0001 again acceptable, or must refs be globally unique across all Ergon customers?) |
| Reporting/dashboard bleed | See §3 | See §3 | Aggregate dollar figures blend across companies | Critical, most visible | See §3/§8 | — |
| Search bleed | See §3 | See §3 | Search results blend across companies | Critical | See §3/§8 | — |
| Cron job pattern | See §3 | See §3 | Establishes an unscoped, service-role, full-table-scan pattern future jobs would likely copy | High (defense-in-depth concern for the *next* job written, more than this specific one) | See §3 | — |
| Shared external-service identity | One Slack workspace, one email-sending identity, one push VAPID identity for the whole deployment | See §1 | Not a data leak, but a brand-identity and support-routing leak — Company B's customers would receive communications that visibly say "Ergon Ops" / route to Ensight's own support email | High for trust/credibility once real second-company customers are involved | See §1 | See §10 |

---

## 5. Existing no-code controls for branding, users, roles, tabs, catalogs, templates, forms, workflow settings, and notifications

The Admin page (`#admin` route, gated by `isAdmin`/`isManagerRole`) is the real, working foundation `PRODUCT_PLAN.md` refers to when it says "Company name/logo administration, editable proposal content, schedule templates, forms, catalog configuration, role access, and notification rules provide a partial no-code foundation." Every panel was inventoried directly:

| Panel | Admin CAN change today, no code required | What's still fixed in code around it |
|---|---|---|
| Company Branding (`main.tsx:17368-17390`) | Company name, logo image | Pre-auth screens (sign-in, password reset, pending-approval) and most server-sent emails still hard-code "Ergon"/"Ergon Ops" regardless of this setting (§1) |
| Team Roster (`17428-17708`) | Invite by email, assign primary + secondary role(s) | The role list itself (`ROLE_KEY_OPTIONS`) is fixed |
| Pending Approvals (`17392-17426`) | Approve/deny/expire sign-ins; assign role + expiry at approval time | Same fixed role list |
| Standard Install Times (`17709-17734`) | Free-text category, hours/unit, notes — genuinely open-ended | None found — this is the one panel the frontend audit found no hidden fixed list behind |
| Project Schedule Templates (`17736-17810`) | Create templates, add/remove/reorder phases, fixed-hours or per-BOM-category duration, optional default role | Default-role dropdown limited to the fixed role list |
| Form Builder — After-Sales Handover / Site Intake (`17812-17834`) | Add/edit/reorder/delete arbitrary form fields on two DB-backed schemas | None found — genuinely no-code |
| Pre-Sales Rules (`17836-17904`) | Map catalog category + node count + cloud-sync flag to baseline hardware quantities | The catalog *category* options it draws from are the fixed `CATALOG_CATEGORY_OPTIONS` array |
| Site Hardware Rules (`17906-17965`) | Item name, qty/unit, notes, active flag, per metric | The metric *types* (FLI/LPR/people-counting/etc.) are fixed |
| Proposal Template (`17967-17978`) | Edit each section's boilerplate text | Explicitly labeled "Seeded from EnSight's real proposal wording" — ships pre-loaded with one company's real legal text |
| Catalog Price Change Requests (`17982-18030`) | Approve/reject proposed catalog price edits | This is a workflow queue, not a settings panel — included because it's the one existing example of the "protected approval" pattern `PRODUCT_PLAN.md` asks for elsewhere (see §7) |
| Admin role/tab/admin-rights grid (`18032-18140+`) | Per-user: primary+secondary role, exact tab-access override, grant/revoke admin, approval status | The role list and the universe of tabs (`ALL_TABS`) are both fixed; only the per-user *assignment* is dynamic |
| Notification Rules (`17677-17703`) | Toggle channel (in-app/email/Slack-Teams/push) and active/inactive, per event type | The panel's own label says "programmable, no code change needed" — true only for channel routing; the event-type catalog itself is fixed server-side in `api/_lib/notificationEvents.js` |

**Assessment**: this is a real, substantive no-code foundation — six of twelve panels (Standard Install Times, Schedule Templates, both Form Builders, Pre-Sales Rules, Site Hardware Rules) have no hidden fixed list behind their primary function, and the remaining six are genuinely useful within the boundary of their fixed underlying list (roles, tabs, catalog categories, hardware metrics, event types). None of these panels currently have any workspace concept — see §1/§8 for what changes once one exists.

---

## 6. Routine configuration that still requires programming

| Configuration change | Why it requires code today | Evidence |
|---|---|---|
| Add, rename, or remove a role | `ROLE_KEY_OPTIONS` is a fixed TypeScript array | `src/main.tsx:16793-16804` |
| Change a role's *default* tab set | `DEFAULT_TABS_BY_ROLE` is fixed; only per-*user* overrides are admin-editable | `src/main.tsx:514-525` |
| Add a new task, purchase-order, or build status | `TASK_STATUS_OPTIONS`/`PURCHASE_ORDER_STATUSES` are fixed unions/arrays with no admin panel | `src/main.tsx:585-591`; `src/persistence.ts:7417-7454,7563` |
| Add a new notification event type | The server-side `HANDLERS`/`SUPPORTED_EVENT_TYPES` allowlist rejects any unrecognized `eventType` with a 400 | `api/_lib/notificationEvents.js:155-409` |
| Add a new catalog category, or category-specific spec fields | `CATALOG_CATEGORY_OPTIONS`/`CATALOG_CATEGORY_FIELDS` are fixed arrays | `src/main.tsx:18543-18558,18598-18644` |
| Add a new site-hardware metric type | `SITE_HARDWARE_METRIC_OPTIONS` is a fixed array | `src/main.tsx:19457-19464` |
| Change the default BOM/shipping "ship to" choices | `DEFAULT_BOM_SHIP_TO = "EnSight Office"` is a hard-coded default among a fixed 3-choice list | `src/main.tsx:467` |
| Change outbound email "from" identity, VAPID push contact, or Slack/Teams destination | All single global environment variables (§1) | `api/_lib/mailer.js`; `api/send-push.js:200`; `api/send-notification-slack.js:101-102` |
| Use a different AI-extraction provider/account, email domain, or Slack workspace per company | Same — single global env vars, no per-company indirection | §1 |
| Change the pre-auth (sign-in/reset/pending-approval) screen's branding | Hard-coded `/ergon-logo.png` and "Ergon" text; renders before `company_branding` can load (no access token yet) | `src/main.tsx:6869,6899,6902,6912,6923,6943,6947,6950,7992` |
| Onboard a second company at all | Requires the `company_branding` singleton constraint, the global `app_admins`/`app_user_roles` tables, and every global-unique constraint listed in §1 to change — none of this is possible through any UI today, by design | §1, §2, §8 |

---

## 7. Configuration that needs preview, versioning, audit history, rollback, or protected approval

`PRODUCT_PLAN.md` states this requirement directly: *"Changes should be previewable, versioned where they affect customer or operational output, audited, and reversible when practical. Published templates should not silently rewrite previously signed quotes, approved projects, or historical records."* Checking the existing no-code panels (§5) against that bar:

| Area | Current Behavior | Evidence | Product Gap | Risk | Proposed Direction | Decision Needed |
|---|---|---|---|---|---|---|
| Proposal Template edits | **Verified 2026-09-08, resolved — editing a template section does NOT retroactively change an already-sent proposal.** `sales_quote_proposals.content_snapshot jsonb` freezes a full copy of the resolved template sections (title/body) at the moment a proposal is created/sent — `createQuoteProposal()` writes exactly the `contentSnapshot` object the caller builds; `main.tsx:5046-5049` builds that object by reading the *current* `proposalTemplateSections` state and copying `{title, body}` for each section at that instant. `persistence.ts:10323-10328`'s own comment confirms the design intent: sections are "baked into content_snapshot at send time... exactly like Submittals freeze their SOW text." Editing the live `proposal_template_sections` table afterward only affects proposals created *after* the edit. | `backend/supabase/migrations/053_sales_quote_proposals.sql:108` (`content_snapshot jsonb not null default '{}'::jsonb`); `src/persistence.ts:10323-10328,10365-10376,10454-10473`; `src/main.tsx:5046-5049,5072-5078` | None — this was already built correctly; the only real gap is that this behavior wasn't previously *documented* anywhere as a guarantee | Low — verified safe | No change needed for this specific concern; if a workspace-scoped version of this table is ever built (§8), preserve the snapshot-at-send pattern exactly as-is | — (resolved) |
| Notification Rules changes | Toggling a channel/active flag writes immediately and globally, with an `updated_at` timestamp but no `changed_by` column confirmed in the schema | `024_notification_rules_engine.sql:8-18` | No audit trail of who changed a notification setting or when, beyond a bare timestamp | Medium | Add `changed_by`/history table if this becomes a per-workspace setting with more than one admin able to change it | — |
| Schedule Template edits | **Verified 2026-09-08, resolved — editing a template does NOT retroactively change tasks already generated from it.** "Generate Schedule" (`handleGenerateSchedule`, `main.tsx:4930-4937`) calls `generateScheduleTasks()`, which reads the *current* `template.phases` at that exact moment and produces independent, standalone `tasks` rows with the phase name and computed hours baked directly into each task's `title`/`description` text (`main.tsx:4905-4926`). A generated task carries no ongoing reference (no `templateId`/`phaseId` column) back to the template it came from, so a later edit to the template only affects the *next* "Generate Schedule" run on a different project, never tasks already created. | `src/main.tsx:4883-4937` | None — same as Proposal Templates, already built correctly; only the guarantee itself was previously undocumented | Low — verified safe | No change needed | — (resolved) |
| Role/tab/admin-rights changes | The Admin grid changes a user's role/tabs/admin status immediately; whether this specific action has its own audit trail (distinct from the `deletion_log`/`task_activity_log` patterns confirmed elsewhere in the app) was not independently confirmed in this audit pass | Not confirmed by any of the three research passes | A permission change is exactly the kind of action `PRODUCT_PLAN.md` asks to be audited | Unknown severity until verified | Targeted follow-up read needed | **Open verification item** |
| Catalog Price Change Requests | This workflow already has the exact "protected approval" shape `PRODUCT_PLAN.md` asks for elsewhere: propose → review → approve/reject, with `catalog_price_change_requests` as the audit record | `046_...sql` (table + workflow, confirmed to exist and gate write access to `product_catalog`) | None — this is a working example, cited here as the pattern worth reusing elsewhere, not a gap | — (this is a positive finding) | Use this exact propose/review/approve shape as the template for any other setting that needs "protected approval" (e.g. a future company-level plan/quota change, if that's ever added) | — |
| Company Branding changes | Name/logo overwrite the singleton row directly; no version history of prior names/logos was found | `039_company_branding.sql:7-17` | Minor — low-stakes setting, but still no rollback if a bad logo upload needs reverting | Low | Not a priority; note for completeness | — |

---

## 8. A safe proposed tenant/workspace architecture (Ergon becomes the first workspace)

This is a **proposal**, not an implementation plan authorized to proceed. It is deliberately staged (see §9) and preserves 100% of existing Ergon/Ensight data as the first, and initially only, workspace.

### Core new concepts

1. **`workspaces`** — one row per customer company. `id`, `name`, `slug`, `status` (active/suspended — no billing fields yet, per `PRODUCT_PLAN.md`'s explicit deferral of product-account billing), `created_at`.
2. **`workspace_members`** — replaces the *scoping* function of today's global `app_user_roles`/`app_admins`. Columns: `workspace_id`, `user_id`, `role_key` (reusing the existing `ROLE_KEY_OPTIONS` vocabulary — no need to redesign roles, just scope them), `is_workspace_admin boolean`. A single `auth.users` row can have membership rows in more than one workspace (see §10 — this needs an explicit decision, but the schema should support it either way since disallowing it later is easy and allowing it later is a bigger migration).
3. **`platform_admins`** — a small, separate table for Ergon's own platform-level staff (today's `app_admins`, or a curated subset of them — decision needed, §10). Platform admins are not automatically workspace admins of any customer's workspace.
4. **A "current workspace" resolution mechanism** — every request needs an answer to "which workspace is this for," used by every RLS policy and every server route. Candidate mechanisms (decision needed, §10):
   - A workspace id embedded in the user's JWT (`app_metadata`), refreshed on workspace switch — most robust, but requires an auth hook or a token-refresh step on switch.
   - A client-supplied header (`X-Workspace-Id`) validated server-side against `workspace_members` on every request — simpler to implement first, still fully safe if validated correctly, matches the existing pattern of routes already trusting nothing from the client without re-verifying it server-side.
5. **A `current_workspace_id()` (or equivalent) `security definer` SQL helper**, mirroring the existing `is_app_admin()`/`has_role()` pattern, used inside RLS policies the same way those functions already are.

### What changes, and what deliberately does not

- **Existing role-based RLS policies (Phase 9, `023`; catalog gate, `033`) are preserved, not replaced.** The workspace predicate is added *alongside* the existing role check (`using (workspace_id = current_workspace_id() and (is_app_admin(...) or has_role(...)))`), so the considerable existing work on "who within a company can do what" is not thrown away or redone.
- **`ROLE_KEY_OPTIONS`, `DEFAULT_TABS_BY_ROLE`, and the rest of §6's fixed vocabulary are not required to change for the first phase of tenancy work.** They become *per-workspace-selectable defaults* eventually (a later, separate product decision — see `PRODUCT_PLAN.md`'s own "Choose enabled modules and navigation" onboarding step), but nothing about tenant isolation itself requires touching them first.
- **The existing "shared records" already confirmed to exist (§2's cross-reference table)** — `clients`, `locations`/`project_locations`, `sales_quotes`/`sales_quote_proposals`, `product_catalog`/`equipment_types`, `projects`, `installed_assets`, `project_documents`, `tasks`, `conversations`/`channels` — are the concrete migration targets for adding `workspace_id`, in the staged order proposed in §9.
- **The records that don't exist yet** (Contact, Opportunity, Billing account, SaaS subscription, Support case) should be designed **with** `workspace_id` from their very first migration, avoiding a retrofit entirely for that half of the model.
- **Singleton/global tables** (`company_branding`, `notification_rules`, `standard_install_times`, `presales_hardware_rules`, `site_hardware_rules`) convert from a bare unique key to a `(workspace_id, key)` composite — same data, same admin UI, just workspace-scoped underneath.
- **Global-uniqueness constraints** (`vendors.name`, `locations.name`, `clients.name`, `team_members` email, `catalog_number`, `po_number`, `sku`, `quote_ref`) become `unique(workspace_id, x)`.
- **Reference-number counters** (`project_ref_counters`, `sales_quote_ref_counters`) get a composite PK `(workspace_id, year)`.
- **Storage** gets a workspace-scoped path-prefix convention (or per-workspace buckets — see §10) and rewritten bucket policies mirroring the existing `channel_id`-prefix pattern already used successfully for `message-attachments` (migrations 100/103/105 — this is proof the codebase already has a working example of path-prefix-based storage scoping to build the workspace version from).
- **External integrations** (email, Slack, push, AI extraction) get a `workspace_integration_settings`-style table; the current global env vars become the literal default values for the first (Ergon) workspace's row, so nothing breaks for Ergon itself on day one.

### Preserving existing Ergon data as the first workspace

The migration path for existing data is one `insert` and a set of `update ... set workspace_id = '<ergon-workspace-id>'` backfills, executed table-group by table-group (§9) — never a single mechanical "add the column everywhere" pass, per `PRODUCT_START_PLAN.md`'s explicit instruction. No existing row's other columns change. No existing user's role/access changes (their `app_user_roles` row becomes a `workspace_members` row in the one Ergon workspace, same role, same access).

---

## 9. Staged migration approach (explicitly not all-at-once)

| Phase | What happens | Risk to existing behavior | Gate before moving to next phase |
|---|---|---|---|
| **0 — this audit** | Read-only; no changes | None (already complete) | Owner review of this document (§10) |
| **1 — Foundational schema, fully additive — ✅ DONE and live-verified, 2026-09-08** | `workspaces`, `workspace_members`, `workspace_member_roles`, `platform_admins` created via `backend/supabase/migrations/115_workspaces_foundation.sql`; one `workspaces` row seeded, holding the existing Ergon/Ensight operational data, and named/slugged **"Ergon Test Workspace" / `ergon-test`** (renamed from an initial "Ergon"/`ensight` via migration `116_rename_first_workspace_to_ergon_test.sql` — see `PRODUCT_PHASE1_PLAN.md` for why: this environment is treated as a general Ergon product-development and testing workspace, not permanently identified as an Ensight customer workspace); every existing `app_admins`/`app_user_roles` row carried over with zero fallback roles assigned; `current_workspace_id()` and five other hardened helper functions built. Full design and live-verification results in `PRODUCT_PHASE1_PLAN.md`. | Zero, confirmed — every existing table's row count matches its pre-migration value exactly, no RLS recursion observed, no console errors | Confirmed: the new tables exist and resolve correctly for the real signed-in admin with zero visible app change |
| **2 — Add nullable `workspace_id` to one table group, backfilled immediately** | Start with the group `PRODUCT_PLAN.md` calls the first-priority handoff area: `sales_quotes`/`sales_quote_proposals`, then `projects`, then the rest of §2's cross-reference table, one group at a time | Low — column is nullable, immediately backfilled to the single "Ergon Test Workspace" id, no RLS or query changes yet | Verify every row in the group has a non-null `workspace_id` before moving to the next group |
| **3 — Tighten RLS for a backfilled group** | Add the workspace predicate *alongside* existing role checks for that group only | Low, if Phase 2 verification passed — with exactly one workspace, `workspace_id = current_workspace_id()` is true for every current user, so no visible behavior change is expected; still verify | Confirm no regression for that table group (existing test suite + a manual pass) before tightening the next group |
| **4 — Convert singleton/global-unique tables** | `company_branding`, `notification_rules`, `standard_install_times`, `presales_hardware_rules`, `site_hardware_rules`, and every global-unique constraint in §1, plus the reference-number counters | Medium — these are structurally singleton today, so this is the most delicate phase; do one table at a time with explicit before/after verification | Confirm existing Admin panels (§5) still work identically against the now-workspace-scoped tables |
| **5 — Storage policy rework** | Path-prefix or per-workspace-bucket convention (decision needed, §10); can run in parallel with Phase 3/4 for the tables that reference the affected buckets | Medium — file access is highly visible to users if broken; stage bucket-by-bucket, verify a real upload/download round-trip after each | Confirm no existing file becomes inaccessible after each bucket's policy rewrite |
| **6 — External integrations become workspace-configurable** | New settings table; existing env vars become the Ergon workspace's default row | Low — additive; Ergon's own integrations keep working via the default row | Confirm Ergon's existing email/Slack/push/AI-extraction flows are unaffected |
| **7 — Server route audit** | Every `api/*.js` route explicitly resolves and enforces `workspace_id`, as defense-in-depth beyond RLS (matching `PRODUCT_START_PLAN.md`'s own instruction to check "server routes that do not resolve and enforce the caller's workspace") | Low if RLS phases (3-4) are already solid; this is belt-and-suspenders, not the primary boundary | — |
| **8 — Guided onboarding flow** | Build the actual "create a new company" journey (`PRODUCT_PLAN.md`'s 10-step onboarding list) — deliberately **last**, after isolation is proven, not first | N/A — no second company should be created before this phase | Independent security review + the specific cross-workspace tests `PRODUCT_START_PLAN.md`'s "Measures of success" calls for, *before* onboarding company #2 |

This order deliberately front-loads the lowest-risk, fully-reversible work (Phase 1) and defers the highest-stakes, most delicate work (singleton-table conversion, Phase 4) until the pattern has already been proven safe on ordinary tables in Phases 2-3.

---

## 10. Working decisions (E, 2026-09-08) and what still needs confirmation

E reviewed the twelve open questions below and set the following as **proposed working defaults** — decided directions to build against, not yet a signal that any implementation has started. Each retains its original question for traceability, followed by the working decision and, where one exists, what is still open underneath it.

1. **Can a user belong to more than one workspace?**
   Original question: affects the `workspace_members` design and whether `team_members`' global email-uniqueness constraint should ever allow the same person to appear under two workspaces.
   **Working decision**: "A user may belong to multiple workspaces and select an active workspace." Adopted as designed in `PRODUCT_PHASE1_PLAN.md`.
   **Still requires confirmation**: the exact UI/UX for switching active workspace (a switcher control, part of a later phase, not Phase 1).

2. **What happens to today's `app_admins`?**
   Original question: `PRODUCT_PLAN.md` requires platform administration and company administration to remain distinct, but didn't say how today's admins map onto that split.
   **Working decision**: "Existing administrators become administrators of the current Ergon/Ensight workspace. Platform-administrator access must remain separate and be granted only to specifically approved designated Ergon product operators." **Verified 2026-09-08**: exactly one account currently holds admin status — `eck1679@gmail.com` — so the workspace-admin migration itself is unambiguous (see §1, and the exact admin roster identified as one of this pass's immediate actions).
   **Still requires confirmation**: the actual platform-administrator roster. This was **deliberately not assigned** as part of this pass, per explicit instruction — platform-admin status remains unassigned until a separate decision is made.

3. **Should currently-global-unique business names remain cross-workspace-unique for any of them?**
   **Working decision**: "Vendors, clients, locations, SKUs, PO numbers, project numbers, quote numbers, and similar business identities should be unique within each workspace, not globally." No exceptions carved out — every one of these becomes strictly per-workspace.

4. **Exact "current workspace" resolution mechanism?**
   **Working decision**: "The server must resolve the active workspace from authenticated workspace membership. Never trust a browser-provided workspace ID without validating membership." This resolves the JWT-vs-header question in favor of a specific pattern: a client MAY suggest a workspace id, but the server always independently validates it against `workspace_members` before honoring it — see `PRODUCT_PHASE1_PLAN.md`'s `resolveActiveWorkspace()` design.

5. **Storage: one bucket per workspace, or shared buckets with a path-prefix convention?**
   **Working decision**: "Use shared Storage buckets with workspace-prefixed paths and enforced Storage policies, unless the technical review finds a specific security limitation." Adopted as the default; the conditional clause is **not yet satisfied** — the technical review it refers to is scoped to Phase 5 (Storage policy rework, `PRODUCT_PHASE1_PLAN.md`'s parent plan §9), not Phase 1, and has not happened yet.

6. **Should the fixed vertical-specific taxonomy become free-text, or ship as templates?**
   **Working decision**: "Ship an Ensight/parking starter configuration and a clean general-business starter. Allow controlled administrator configuration rather than making every value unrestricted free text." Adopted; this settles the *direction* (templates, not unrestricted free text) but not the specifics.
   **Still requires confirmation**: exactly which fields become "controlled configuration" (a bounded, admin-managed list) vs. remain fixed, and what the "clean" general-business starter's own default catalog/taxonomy looks like. This is onboarding/template design work, out of scope for Phase 1.

7. **Dead seed-data array and the live Emerald Queen fallback.**
   **Working decision**: "Remove the unused source-code array containing real client information. Replace the still-active Emerald Queen fallback with clearly fictional sample data, but first confirm that removing or replacing it will not affect existing production records or workflows." **The requested confirmation is now complete** — see the updated §1 rows above for the full verification evidence. Both changes are confirmed safe. **The code changes themselves have not been made** — this pass was scoped to verification only.

8. **External integrations: shared/default provider, or bring-your-own from day one?**
   **Working decision**: "Provide platform-managed email and push defaults. Configure Slack/Teams per workspace. Keep paid AI disabled or customer-provided until usage limits and future product billing are designed." This is a mixed answer, not one uniform policy — email/push get a platform default (matching today's behavior for the first workspace), Slack/Teams is per-workspace from the start (no shared default), and paid AI extraction is explicitly gated behind future usage-limit and billing design, not part of any near-term phase.
   **Still requires confirmation**: the usage-limit and billing design itself — explicitly deferred, not a Phase 1 concern.

9. **Reference-number scope: per-workspace or global?**
   **Working decision**: "Use per-workspace reference numbering, allowing each company to have its own PRJ-2026-0001, quote numbers, PO numbers, and similar references." Settles the question directly — every company starts its own numbering.

10. **Sequencing against the Sales-replacement work.**
    **Working decision**: "Sales workflow research, journey mapping, and prototypes may proceed in parallel. [Production] Sales schema work that creates or changes shared records must follow the approved workspace ownership model." In effect: discovery/design/prototyping work is unblocked immediately; any actual schema change to a shared record (per §2's cross-reference table) must wait for the workspace ownership model to exist first, so new Sales-side tables (e.g. a future Opportunity or Contact table) should be designed with `workspace_id` from their first migration rather than being retrofitted later.

11. **Proposal Template / Schedule Template history behavior.**
    **Working decision**: "Directly verify whether editing Proposal Templates or Schedule Templates changes previously generated proposals, signed quotes, or existing project schedules. Historical records must never change silently." **This verification is now complete** — see the updated §7 rows above. Both are confirmed safe by direct code trace: proposals freeze a full content snapshot at send time, and generated schedule tasks are independent rows with no live reference back to their template. No further action needed on this item.

12. **Reuse the propose/review/approve pattern, or design fresh?**
    **Working decision**: "Reuse the existing propose/review/approve technical pattern where appropriate, but give workspace administration and Billing Review their own design review before implementation." Adopted — the Catalog Price Change Requests shape (§7) is confirmed as the reusable technical pattern, but workspace-admin UI and Billing Review each get their own dedicated design pass rather than being assumed to fit the same shape without review.

**What this section does not yet decide**: none of the twelve working decisions above authorize any schema, RLS, API, or workflow change. `PRODUCT_PHASE1_PLAN.md` is the first concrete implementation plan built against these defaults, and it is itself pending review — see that document's own "Decisions still needed before implementation" section for what remains open specifically for Phase 1.

---

## Summary

Every one of the ten requested areas was audited against the real, current codebase — 114 migrations read in full, all 8 server routes and 8 shared helpers, and the two main frontend files (36,000 combined lines) — with file:line citations throughout, not inferred from feature names. The headline finding, confirmed independently by all three research passes: **zero tables in this schema have any company/workspace/tenant column, the two authorization primitives (`app_admins`, `app_user_roles`) are both global with no company dimension, and the dominant RLS pattern across most business tables is still fully open to any signed-in user** — a deliberate and well-reasoned design for a single-company internal tool, but one that would mix or expose data across companies immediately if a second company were added to this database without the staged work proposed in §§8-9.

Nothing in this document has been implemented. §10 now records working decisions for all twelve original open questions (E, 2026-09-08); two of §7's previously-open verification items are directly confirmed (Proposal/Schedule Template edits do not retroactively change historical records); the exact admin roster in §1 is directly confirmed (one account, `eck1679@gmail.com`); and both proposed source-code cleanups (the dead client-data array, the Emerald Queen fallback) are confirmed safe to make, though not yet made. `PRODUCT_PHASE1_PLAN.md` carries these working decisions forward into the first concrete, not-yet-implemented implementation plan.
